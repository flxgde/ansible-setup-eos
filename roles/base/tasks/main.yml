- name: Update pacman cache
  pacman:
    update_cache: yes

- name: Install base packages
  pacman:
    name:
      - git
      - neovim
      - htop
      - zsh
      - base-devel
    state: installed


---
- name: Create .local/bin directory
  ansible.builtin.file:
    path: "{{ ansible_env.HOME }}/.local/bin"
    state: directory
    mode: '0755'

- name: Symlink tmux-sessionizer
  ansible.builtin.file:
    src: "{{ ansible_env.HOME }}/dotfiles/.local/bin/tmux-sessionizer"
    dest: "{{ ansible_env.HOME }}/.local/bin/tmux-sessionizer"
    state: link
    force: yes

- name: Symlink tmux-switch-session
  ansible.builtin.file:
    src: "{{ ansible_env.HOME }}/dotfiles/.local/bin/tmux-switch-session"
    dest: "{{ ansible_env.HOME }}/.local/bin/tmux-switch-session"
    state: link
    force: yes
